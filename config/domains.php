<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Мультидоменность
    |--------------------------------------------------------------------------
    |
    | Настройки для работы с несколькими доменами
    |
    */

    // Включить мультидоменность
    'enabled' => env('MULTI_DOMAIN_ENABLED', true),

    // Домен по умолчанию (если не найден в БД)
    'default' => env('APP_DOMAIN', 'bestway.site'),

    // Кэширование настроек доменов (в минутах)
    'cache_ttl' => env('DOMAIN_CACHE_TTL', 60),

    // Игнорировать поддомены при поиске
    'ignore_subdomains' => [
        'www',
        'api',
        'admin',
        'app',
        'cdn',
        'static',
    ],

    // Настройки для разных окружений
    'environments' => [
        'local' => [
            'default' => 'bestway.test',
            'cache_ttl' => 0, // Отключить кэш в локальной разработке
        ],
        'testing' => [
            'default' => 'bestway.test',
            'cache_ttl' => 0,
        ],
    ],
];